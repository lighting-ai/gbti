# 复制为 .env 或 .env.production（不要提交 .env 到 git）
#
# 可选：生产站点根 URL，不要末尾斜杠。
# 设置后，前端会在运行时补全 canonical、og:url、og:image / twitter:image 的绝对地址
#（对少数只认绝对地址的爬虫/分享预览更友好）。
# 不设置则完全使用相对路径，任意域名 / fork 部署均可直接使用，无需改代码。
#
# 导出/分享结果图底部版权行（© 年份 · 主机名，无协议）：
# http(s) 下用当前页 hostname；file:// 等则用此处解析出的域名或默认 gbti.fundpulse.shop。
#
# VITE_SITE_ORIGIN=https://gbti.fundpulse.shop
